# Persistent decryption keys + decrypted logs (bind-mounted into the
# TWS container; contains sensitive material).
logs/

# Secrets
docker/tws.secrets

# TWS installation (downloaded)
tws-jars

# IBC source (cloned from upstream)
IBC

# Downloaded dependencies (downloaded at build time)
docker/IBC/lib/*.jar

# Patches folder
ibc-patches/lib

# IBC build artifacts (when using IBC from upstream)
IBC/resources/IBC.jar
IBC/target/
IBC/dist/

# Persistent TWS data dir (bind-mounted into the container). Contains
# encrypted logs, the keys archive, decrypted logs, and TWS settings.
jts/
